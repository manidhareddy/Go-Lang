# Networking with http

The Go standard library has many packages for making web servers:
- client & server sockets
- route multiplexing
- HTTP and HTML, including HTML templates


import "net/http"

- Banding handler against a route